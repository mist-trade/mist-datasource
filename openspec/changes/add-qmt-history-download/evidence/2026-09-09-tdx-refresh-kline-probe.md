# Evidence: TDX refresh_kline 生产 probe（task 3.1）

- 时间：2026-09-09 ~02:36 北京时间（盘外）
- 通道：`POST /v1/raw/tdx/call`（raw-diagnostics，黑名单守卫期）→ 终端 HTTP 17709
- 执行：2 标的（880003.SH / 000688.SH）× 3 基础周期（1m/5m/1d）× 3 次
  `refresh_kline`，随后各 1 次 `get_market_data`（start=end=20260908）读回验证

## 延迟与结果矩阵

| symbol | period | refresh_kline 延迟 ms（×3） | ErrorId | 9/8 读回条数 |
|--------|--------|------------------------------|---------|--------------|
| 880003.SH | 1m | 114 / 19 / 18 | 0 | 240 |
| 880003.SH | 5m | 19 / 17 / 20 | 0 | 48 |
| 880003.SH | 1d | 110 / 103 / 96 | 0 | 1 |
| 000688.SH | 1m | 20 / 17 / 18 | 0 | 240 |
| 000688.SH | 5m | 16 / 16 / 16 | 0 | 48 |
| 000688.SH | 1d | 110 / 114 / 110 | 0 | 1 |

## 结论

1. **全部成功**：12 次 refresh 全部 `ErrorId=0`、`Msg="refresh kline cache success."`
   （成功形状 = ErrorId 判定，与设计 D8 一致；消息字段实测 `Msg`，文档样例为
   `Error`，归一化不依赖消息字段名）。
2. **延迟量级**：16–114ms——比设计默认超时（600s）低三个数量级。首次调用偶发
   ~110ms（首触开销），后续稳定 16–20ms。**timeout 默认值维持 600s 不变**
   （为将来首装/大范围场景保留余量），无收紧必要。
3. **读回全部符合预期**：1m=240、5m=48、1d=1，且 start/end=20260908 过滤生效
   （日期粒度过滤可用）。
4. **两终端均有完整 9/8 数据**（880003 今晚实时流断过但历史可取；000688 此前
   QMT/TDX 均取不到的 1m 现经 TDX 可取——与昨日补数实测一致）。

## 待办（本 probe 未覆盖）

- **盘中行为抽查**（官方文档："盘中下载 1m/5m 只能到截止上个交易日"）——需在
  交易日 09:30–15:00 时段内执行同款 probe 验证，**待盘中补测**。
- 大范围首装延迟（如 30 交易日 × 多标的）未测——当前全部为增量/小范围场景，
  首装场景出现时补测并复核 timeout 默认值。
